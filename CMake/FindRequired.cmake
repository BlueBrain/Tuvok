
# generated by Buildyard, do not edit. Sets FOUND_REQUIRED if all required
# dependencies are found. Used by Buildyard.cmake
set(FIND_REQUIRED_FAILED)
find_package(ZLIB 1.2 QUIET)
if(NOT ZLIB_FOUND AND NOT ZLIB_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} ZLIB")
endif()
find_package(Qt4 4.6 COMPONENTS QtCore QtGui QtXml QtOpenGL QUIET)
if(NOT Qt4_FOUND AND NOT QT4_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} Qt4")
endif()
find_package(BISON  QUIET)
if(NOT BISON_FOUND AND NOT BISON_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} BISON")
endif()
find_package(FLEX  QUIET)
if(NOT FLEX_FOUND AND NOT FLEX_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} FLEX")
endif()
find_package(OpenGL  QUIET)
if(NOT OpenGL_FOUND AND NOT OPENGL_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} OpenGL")
endif()
if(FIND_REQUIRED_FAILED)
  set(FOUND_REQUIRED FALSE)
else()
  set(FOUND_REQUIRED TRUE)
endif()